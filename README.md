# pdftool

A flutter based cross platform app that helps capture images convert them into PDFs.
Merge existing PDFs into new ones, Split a PDF into multiple PDFs based on breakpoints given.
Convert images to PDFs and encrypt PDFs.
Uses the Syncfusion Package.
Uses Firebase Auth
